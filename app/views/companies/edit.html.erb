<% provide(:title, @company.name) %>
<div id="reg_form" class="bg_form">
  <h1>Editar empresa</h1>

	<%= render 'form' %>
	<hr>
	<%= link_to 'Mostrar', @company %> |
	<%= link_to 'Regresar', companies_path %>
</div>